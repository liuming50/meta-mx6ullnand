# Set the U-Boot configuration
UBOOT_CONFIG = "nand"

# Add initramfs support
INITRAMFS_IMAGE = "core-image-initramfs-boot"
INITRAMFS_IMAGE_BUNDLE = "1"
INITRAMFS_FSTYPES = "cpio.gz"

ACCEPT_FSL_EULA = "1"
